language: objective-c
osx_image: xcode8.3
xcode_workspace: SwiftQ.xcworkspace
xcode_scheme: SwiftQ

script:
    - xcodebuild -workspace SwiftQ.xcworkspace -scheme SwiftQ -destination 'platform=iOS Simulator,name=iPhone 7 Plus,OS=10.3.1' -enableCodeCoverage YES build test
    
after_success:
    - bash <(curl -s https://codecov.io/bash)